@{
    /* The standard scaffold will use the same layout as the login/register pages but in
       this design they are full screen without nav (i.e. not authenticated) where the
       management pages should show the standard app nav (i.e. are authenticated).
       So we override with the Shared App _Layout directly

       fyi ParentLayout is the Layout value from _ViewStart.cshtml
    */
    // if (ViewData.TryGetValue("ParentLayout", out var parentLayout) && parentLayout != null)
    // {
    //     Layout = parentLayout.ToString();
    // }
    // else
    // {
    Layout = "/Pages/Shared/_Layout.cshtml";
    //}
}

@RenderBody()

@section TopNav {
    <partial name="_ManageNav" />
}


@section Scripts {
    @RenderSection("Scripts", required: false)
}
